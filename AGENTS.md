# DevStudio 开发约定

- 始终在当前项目中开发，不要创建无关项目。
- 修改前先了解现有结构，保留用户已有改动。
- 优先完成可直接使用的结果，保持移动端体验良好。
- 不写单元测试，不执行 build 或 test。
- 代码注释始终使用中文。
- 完成后简洁说明改了什么，以及是否需要用户执行额外操作。
- 每次修改功能、接口、配置、数据结构、部署方式或重要行为后，都要同步更新 `doc.md` 和 `README.md`。
- `doc.md` 用于详细记录当前实现、架构、接口、边界和已知限制，以便 Agent 后续迭代。
- `README.md` 用于简洁记录项目概况、主要功能、启动方式和常用配置。
